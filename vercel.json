{
  "version": 2,
  "buildCommand": "cd backend && npm install && npm run build",
  "installCommand": "cd backend && npm install",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "admin/**"
    }
  }
}
