# ngrok 浏览器验证问题解决方案

## 问题
ngrok免费版会显示浏览器验证页面，这可能阻止微信服务器的验证请求。

## 解决方案

### 方案1：使用ngrok的静态域名（推荐）

如果您有ngrok账号，可以配置静态域名：
```bash
ngrok http 3000 --domain=your-static-domain.ngrok-free.app
```

### 方案2：使用明文模式（临时测试）

如果安全模式有问题，可以先用明文模式测试：

1. **在微信公众平台**：
   - 选择"明文模式"（而不是"安全模式"）
   - EncodingAESKey可以留空或删除

2. **在后端 `.env` 文件**：
   ```env
   OA_ENCODING_AES_KEY=  # 留空
   ```

3. **修改代码以支持明文模式**（如果需要）

### 方案3：使用其他内网穿透工具

- **localtunnel**: `npx localtunnel --port 3000`
- **serveo**: `ssh -R 80:localhost:3000 serveo.net`
- **cloudflare tunnel**: 免费且稳定

### 方案4：部署到真实服务器

如果可能，部署到有真实域名的服务器（推荐生产环境使用）

## 当前建议

由于ngrok免费版的限制，建议：

1. **先尝试明文模式**：
   - 在微信平台选择"明文模式"
   - 重新提交配置
   - 看是否能通过验证

2. **如果明文模式成功**：
   - 说明是EncodingAESKey的问题
   - 可以后续再切换到安全模式

3. **如果仍然失败**：
   - 检查是否有微信服务器的请求到达（查看ngrok日志）
   - 考虑使用其他内网穿透工具
